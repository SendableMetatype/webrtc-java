# webrtc-java fork maven repository

Maven layout serving the SendableMetatype webrtc-java fork builds
(sendAsync, ICE selected candidate pair bridge). Consumed as:

    maven("https://raw.githubusercontent.com/SendableMetatype/webrtc-java/maven-repo/")

Artifacts are built by this repository's GitHub Actions workflow.
